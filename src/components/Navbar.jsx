import React, {useState} from 'react';

const Navbar = () =>{
    return(
        <nav className="navbar navbar-light bg-light justify-content-between">
         <a className="navbar-brand">Train Buddy</a>
        </nav>
    )
}

export default Navbar;